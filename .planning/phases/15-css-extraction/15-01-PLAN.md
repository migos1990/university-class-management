---
phase: 15-css-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/styles.css
  - views/partials/header.ejs
  - views/sca/student-lab.ejs
  - views/sca/finding-detail.ejs
  - views/sca/instructor.ejs
  - views/sca/answer-key.ejs
  - views/sca/student-detail.ejs
  - views/dast/student-lab.ejs
  - views/dast/scenario-detail.ejs
  - views/dast/instructor.ejs
autonomous: true
requirements:
  - CSS-01

must_haves:
  truths:
    - "public/styles.css exists and is served by Express static middleware"
    - "header.ejs links to /styles.css instead of containing inline base CSS"
    - "SCA and DAST templates have no inline <style> blocks (shared classes come from styles.css)"
    - "All pages that include header.ejs render visually identical to before the refactor"
  artifacts:
    - path: "public/styles.css"
      provides: "All shared CSS (header base + severity/classification/status badges + progress bars + action buttons + cards + precondition boxes) plus page-specific CSS from all templates"
      contains: ".sev-Critical"
    - path: "views/partials/header.ejs"
      provides: "Stylesheet link instead of 409-line inline style block"
      contains: '<link rel="stylesheet" href="/styles.css">'
  key_links:
    - from: "views/partials/header.ejs"
      to: "public/styles.css"
      via: "<link> tag in <head>"
      pattern: 'href="/styles.css"'
    - from: "views/sca/student-lab.ejs"
      to: "public/styles.css"
      via: "header.ejs partial inclusion"
      pattern: "No inline <style> block"
---

<objective>
Create public/styles.css containing ALL shared and page-specific CSS extracted from header.ejs and all 16 template files. Update header.ejs to link the stylesheet. Remove inline style blocks from the 8 SCA and DAST templates.

Purpose: Eliminate CSS duplication across 17 templates by centralizing all styles into a single shared stylesheet. This plan creates the complete stylesheet (including CSS for VM/Pentest/Admin templates cleaned in Plan 02) and cleans the first batch of templates.
Output: public/styles.css (~500-700 lines), updated header.ejs, 8 cleaned SCA/DAST templates.
</objective>

<execution_context>
@/Users/juliegoyette/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliegoyette/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-css-extraction/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public/styles.css and update header.ejs</name>
  <files>public/styles.css, views/partials/header.ejs</files>
  <action>
Create public/styles.css by extracting CSS from ALL template files (not just SCA/DAST -- include VM, Pentest, Admin too so Plan 02 only needs to delete style blocks).

The stylesheet must contain ALL of the following sections in this order:

1. **Base styles from header.ejs** (lines 8-409): The entire 400-line block including reset, body, app-layout, sidebar, nav, user-profile, main-content, security-status, badges, page-content, cards, buttons, alerts, tables, page-header, grids, stat-cards. Copy verbatim.

2. **Severity badges** (from 14 files): Normalize to canonical values:
   ```css
   .sev-Critical  { background:#ffe0e0; color:#c0392b; }
   .sev-High      { background:#fff0e0; color:#c0732a; }
   .sev-Medium    { background:#fffbe0; color:#9a7d0a; }
   .sev-Low       { background:#e8f8e8; color:#1e8449; }
   .sev-Info      { background:#e8f0ff; color:#1a5276; }
   .badge-sm      { display:inline-block; padding:2px 8px; border-radius:4px; font-size:0.75rem; font-weight:600; }
   ```

3. **Classification badges** (from 6 files):
   ```css
   .cls-confirmed      { background:#d4edda; color:#155724; }
   .cls-false_positive { background:#f8d7da; color:#721c24; }
   .cls-needs          { background:#fff3cd; color:#856404; }
   .cls-none           { background:#e9ecef; color:#6c757d; }
   ```

4. **VM status and source badges** (from 3 files):
   ```css
   .status-open        { background:#fde8e8; color:#a93226; }
   .status-in_progress { background:#fef9e7; color:#9a7d0a; }
   .status-resolved    { background:#e8f8e8; color:#1e8449; }
   .status-wont_fix    { background:#ececec; color:#555; }
   .src-sca    { background:#e8f0ff; color:#1a5276; }
   .src-dast   { background:#fde8f0; color:#7b1f42; }
   .src-pentest{ background:#f0e8ff; color:#5b2c8c; }
   .src-manual { background:#f0f0f0; color:#444; }
   ```

5. **Student activity status badges** (from instructor.ejs):
   ```css
   .status-active     { background:#d4edda; color:#155724; }
   .status-inactive   { background:#fff3cd; color:#856404; }
   .status-notstarted { background:#e9ecef; color:#6c757d; }
   ```

6. **Progress bars** (from 3 files):
   ```css
   .progress-bar-wrap { background:#e9ecef; border-radius:4px; height:6px; margin-top:4px; }
   .progress-bar-fill { height:6px; border-radius:4px; background:#002855; }
   .progress-outer    { background:#e9ecef; border-radius:8px; height:12px; }
   .progress-inner    { height:12px; border-radius:8px; background:#002855; transition:width 0.3s; }
   ```

7. **Action buttons** (from 2 files):
   ```css
   .action-btn   { padding:4px 10px; border:none; border-radius:4px; cursor:pointer; font-size:0.8rem; }
   .btn-import   { background:#002855; color:#fff; }
   .btn-imported { background:#e9ecef; color:#6c757d; cursor:default; }
   ```

8. **Finding and scenario cards** (from 2 files):
   ```css
   .finding-card          { border:1px solid #dee2e6; border-radius:8px; padding:1rem 1.25rem; margin-bottom:1rem; background:#fff; }
   .finding-card.done     { border-left:4px solid #28a745; }
   .finding-card.pending  { border-left:4px solid #ffc107; }
   .scenario-card         { border:1px solid #dee2e6; border-radius:8px; padding:1.25rem; margin-bottom:1rem; background:#fff; }
   .scenario-card.done    { border-left:4px solid #28a745; }
   .scenario-card.locked  { opacity:0.6; }
   ```

9. **Precondition boxes** (from 2 files):
   ```css
   .precondition-box { border-radius:6px; padding:0.6rem 0.9rem; font-size:0.9rem; }
   .pre-met   { background:#d4edda; color:#155724; border:1px solid #c3e6cb; }
   .pre-unmet { background:#fff3cd; color:#856404; border:1px solid #ffeeba; }
   ```

10. **Page-specific styles**: Read EVERY template file listed below and extract ALL remaining page-specific CSS classes into named sections. Read each file FULLY to capture everything:
    - views/sca/answer-key.ejs (~73 lines of .ak-* classes)
    - views/sca/student-lab.ejs (any remaining page-specific classes beyond shared ones)
    - views/sca/finding-detail.ejs (any page-specific classes)
    - views/sca/instructor.ejs (any page-specific classes)
    - views/sca/student-detail.ejs (any page-specific classes)
    - views/dast/student-lab.ejs (any page-specific classes)
    - views/dast/scenario-detail.ejs (any page-specific classes)
    - views/dast/instructor.ejs (any page-specific classes)
    - views/vm/student-lab.ejs (any page-specific classes)
    - views/vm/vuln-detail.ejs (any page-specific classes)
    - views/vm/instructor.ejs (any page-specific classes including modal styles)
    - views/pentest/student-lab.ejs (any page-specific classes)
    - views/pentest/engagement-detail.ejs (any page-specific classes)
    - views/pentest/report-builder.ejs (any page-specific classes)
    - views/pentest/instructor.ejs (any page-specific classes)
    - views/admin/security-panel.ejs (~139 lines of .security-* classes)

Use section comment headers like `/* ===== Section Name ===== */` for organization.

Then update views/partials/header.ejs:
- Replace the entire first `<style>...</style>` block (lines 7-409) with: `<link rel="stylesheet" href="/styles.css">`
- KEEP the conditional Prism block intact (lines 410-420):
  ```
  <% if (locals.needsPrism) { %>
  <link rel="stylesheet" href="/vendor/prism/prism-one-dark.css">
  ...
  <style>
    .line-highlight { ... }
  </style>
  <% } %>
  ```
  This conditional 3-line Prism override MUST stay inline in header.ejs.

CRITICAL: Do NOT touch login.ejs, error.ejs, or mfa-verify.ejs -- they are standalone pages with their own <head>.
  </action>
  <verify>
    <automated>test -f public/styles.css && grep -q '.sev-Critical' public/styles.css && grep -q 'href="/styles.css"' views/partials/header.ejs && npm test</automated>
  </verify>
  <done>public/styles.css exists with all shared + page-specific CSS from all 16 templates. header.ejs links to /styles.css. Prism conditional block preserved. npm test passes (server boots, pages render).</done>
</task>

<task type="auto">
  <name>Task 2: Remove inline style blocks from SCA and DAST templates</name>
  <files>views/sca/student-lab.ejs, views/sca/finding-detail.ejs, views/sca/instructor.ejs, views/sca/answer-key.ejs, views/sca/student-detail.ejs, views/dast/student-lab.ejs, views/dast/scenario-detail.ejs, views/dast/instructor.ejs</files>
  <action>
For each of the 8 SCA and DAST template files, delete the entire `<style>...</style>` block. The CSS classes are now served via public/styles.css linked from header.ejs.

Process each file:
1. Read the file
2. Locate the `<style>` block (typically near the top, after the header partial include)
3. Delete the entire block from `<style>` to `</style>` inclusive
4. Verify no other `<style>` tags remain in the file (some files may have only one block)
5. Do NOT modify any HTML, EJS logic, or JavaScript in the file -- only remove the CSS block

Files to process:
- views/sca/student-lab.ejs -- remove ~16-line style block (severity/classification/finding-card/progress classes)
- views/sca/finding-detail.ejs -- remove ~10-line style block (severity/classification/badge-sm classes)
- views/sca/instructor.ejs -- remove ~22-line style block (severity/classification/progress/action-btn/status classes)
- views/sca/answer-key.ejs -- remove ~73-line style block (severity/classification/badge-sm + ak-* classes)
- views/sca/student-detail.ejs -- remove ~11-line style block (severity/classification/badge-sm classes)
- views/dast/student-lab.ejs -- remove ~13-line style block (severity/scenario-card/precondition classes)
- views/dast/scenario-detail.ejs -- remove ~10-line style block (severity/badge-sm/precondition classes)
- views/dast/instructor.ejs -- remove ~13-line style block (severity/progress/action-btn classes)

After all deletions, verify with grep that no `<style>` tags remain in any of the 8 files.
  </action>
  <verify>
    <automated>! grep -l '<style>' views/sca/student-lab.ejs views/sca/finding-detail.ejs views/sca/instructor.ejs views/sca/answer-key.ejs views/sca/student-detail.ejs views/dast/student-lab.ejs views/dast/scenario-detail.ejs views/dast/instructor.ejs 2>/dev/null && npm test</automated>
  </verify>
  <done>All 8 SCA and DAST template files have zero inline style blocks. grep confirms no <style> tags. npm test passes.</done>
</task>

</tasks>

<verification>
- `public/styles.css` exists and contains severity badges, classification badges, status badges, progress bars, action buttons, cards, precondition boxes, header base styles, and all page-specific styles
- `views/partials/header.ejs` has `<link rel="stylesheet" href="/styles.css">` instead of 409-line inline style block
- Prism conditional block in header.ejs is preserved unchanged
- Zero `<style>` tags in any SCA or DAST template file
- `login.ejs`, `error.ejs`, `mfa-verify.ejs` are untouched
- `npm test` passes (server boots, pages render correctly)
</verification>

<success_criteria>
- public/styles.css serves all shared and page-specific CSS via Express static middleware
- header.ejs uses <link> tag instead of inline <style> block
- 8 SCA/DAST templates have no inline <style> blocks
- npm test passes confirming pages still render
</success_criteria>

<output>
After completion, create `.planning/phases/15-css-extraction/15-01-SUMMARY.md`
</output>
