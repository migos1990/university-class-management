---
phase: 15-css-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - views/vm/student-lab.ejs
  - views/vm/vuln-detail.ejs
  - views/vm/instructor.ejs
  - views/pentest/student-lab.ejs
  - views/pentest/engagement-detail.ejs
  - views/pentest/report-builder.ejs
  - views/pentest/instructor.ejs
  - views/admin/security-panel.ejs
autonomous: true
requirements:
  - CSS-01

must_haves:
  truths:
    - "VM templates have no inline <style> blocks"
    - "Pentest templates have no inline <style> blocks"
    - "Admin security-panel has no inline <style> block"
    - "All authenticated pages render visually identical to before the refactor"
    - "No leftover empty <style></style> tags in any template"
  artifacts:
    - path: "views/vm/student-lab.ejs"
      provides: "VM student lab template without inline CSS"
    - path: "views/pentest/student-lab.ejs"
      provides: "Pentest student lab template without inline CSS"
    - path: "views/admin/security-panel.ejs"
      provides: "Admin security panel template without inline CSS"
  key_links:
    - from: "views/vm/student-lab.ejs"
      to: "public/styles.css"
      via: "header.ejs partial -> <link> tag"
      pattern: "No inline <style> block"
    - from: "views/admin/security-panel.ejs"
      to: "public/styles.css"
      via: "header.ejs partial -> <link> tag"
      pattern: "No inline <style> block"
---

<objective>
Remove inline <style> blocks from the remaining 8 templates (VM, Pentest, Admin) and verify the complete CSS extraction is clean across the entire codebase.

Purpose: Complete the CSS-01 requirement by removing all remaining inline <style> blocks. All CSS classes were already added to public/styles.css in Plan 01.
Output: 8 cleaned templates, full codebase verification.
</objective>

<execution_context>
@/Users/juliegoyette/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliegoyette/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-css-extraction/15-RESEARCH.md
@.planning/phases/15-css-extraction/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove inline style blocks from VM, Pentest, and Admin templates</name>
  <files>views/vm/student-lab.ejs, views/vm/vuln-detail.ejs, views/vm/instructor.ejs, views/pentest/student-lab.ejs, views/pentest/engagement-detail.ejs, views/pentest/report-builder.ejs, views/pentest/instructor.ejs, views/admin/security-panel.ejs</files>
  <action>
For each of the 8 remaining template files, delete the entire `<style>...</style>` block. All CSS classes were already extracted to public/styles.css in Plan 01.

Process each file:
1. Read the file
2. Locate the `<style>` block
3. Delete the entire block from `<style>` to `</style>` inclusive
4. Verify no other `<style>` tags remain in the file
5. Do NOT modify any HTML, EJS logic, or JavaScript -- only remove the CSS block

Files to process:
- views/vm/student-lab.ejs -- remove ~16-line style block (severity/status/source/finding-card classes)
- views/vm/vuln-detail.ejs -- remove ~19-line style block (severity/status/source/badge-sm classes)
- views/vm/instructor.ejs -- remove ~18-line style block (severity/status/source/modal classes)
- views/pentest/student-lab.ejs -- remove ~14-line style block (severity/badge-sm/phase/engagement classes)
- views/pentest/engagement-detail.ejs -- remove ~7-line style block (severity/badge-sm/finding-row classes)
- views/pentest/report-builder.ejs -- remove ~18-line style block (severity/badge-sm/report-section classes)
- views/pentest/instructor.ejs -- remove ~11-line style block (severity/badge-sm/action-btn classes)
- views/admin/security-panel.ejs -- remove ~139-line style block (security-grid/security-card/toggle/modal classes)

After all deletions, verify with grep.
  </action>
  <verify>
    <automated>! grep -l '<style>' views/vm/student-lab.ejs views/vm/vuln-detail.ejs views/vm/instructor.ejs views/pentest/student-lab.ejs views/pentest/engagement-detail.ejs views/pentest/report-builder.ejs views/pentest/instructor.ejs views/admin/security-panel.ejs 2>/dev/null && npm test</automated>
  </verify>
  <done>All 8 VM, Pentest, and Admin template files have zero inline style blocks. npm test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Final codebase-wide verification and cleanup</name>
  <files>public/styles.css</files>
  <action>
Run a codebase-wide grep to verify the CSS extraction is complete:

1. Run `grep -rn '<style>' views/` to list ALL remaining `<style>` tags in the views directory.

2. Verify the ONLY remaining `<style>` tags are in:
   - views/login.ejs (standalone page -- expected, do not touch)
   - views/error.ejs (standalone page -- expected, do not touch)
   - views/mfa-verify.ejs (standalone page -- expected, do not touch)
   - views/partials/header.ejs (Prism conditional 3-line override only -- expected, do not touch)

3. If any OTHER template still has a `<style>` tag, remove it.

4. Verify public/styles.css is loadable: `curl -s http://localhost:3000/styles.css | head -5` (requires server to be running, or just confirm the file starts with expected CSS).

5. Run `npm test` to confirm all smoke tests pass.

6. Run `npm run test:integration` for the full integration suite.

7. Run `npm run lint` and `npm run format:check` to ensure the modified files pass code quality checks. If lint or format issues exist in the modified EJS files, they are expected (EJS is excluded from Prettier per Phase 14 decision). Only fix issues in public/styles.css if any.
  </action>
  <verify>
    <automated>npm test && npm run test:integration</automated>
  </verify>
  <done>Codebase-wide grep confirms only standalone pages and Prism conditional retain inline styles. All tests pass (smoke + integration). CSS-01 requirement fully satisfied: common CSS patterns are in public/styles.css and referenced from templates instead of inline style blocks.</done>
</task>

</tasks>

<verification>
- `grep -rn '<style>' views/` returns ONLY: login.ejs, error.ejs, mfa-verify.ejs, header.ejs (Prism conditional)
- Zero `<style>` tags in any SCA, DAST, VM, Pentest, or Admin template
- `npm test` passes (server boots, pages render)
- `npm run test:integration` passes (full integration suite)
- public/styles.css is served at /styles.css by Express
</verification>

<success_criteria>
- All 16 template files (SCA 5 + DAST 3 + VM 3 + Pentest 4 + Admin 1) have zero inline <style> blocks
- Only standalone pages (login, error, mfa-verify) and Prism conditional in header.ejs retain inline styles
- All test suites pass
- CSS-01 requirement is complete
</success_criteria>

<output>
After completion, create `.planning/phases/15-css-extraction/15-02-SUMMARY.md`
</output>
