---
phase: 08-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/helpers.js
  - test/sca-review.test.js
  - test/answer-key-gating.test.js
  - test/api-auth.test.js
  - routes/sca.js
  - package.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "Running npm run test:integration against a live server executes all three test files and reports results"
    - "SCA review submission test posts a classification as student, then verifies it persists in the finding detail page"
    - "Answer key gating test proves students get 403 and professors get 200 on GET /sca/answer-key"
    - "API auth test proves unauthenticated requests to /api/instructor-message (GET and POST) and /api/summary get 302"
  artifacts:
    - path: "test/helpers.js"
      provides: "Shared HTTP helpers for integration tests"
      exports: ["request", "getSessionCookie", "loginAs", "BASE_URL"]
    - path: "test/sca-review.test.js"
      provides: "TEST-01 integration test"
      contains: "SCA Review"
    - path: "test/answer-key-gating.test.js"
      provides: "TEST-02 integration test"
      contains: "Answer Key"
    - path: "test/api-auth.test.js"
      provides: "TEST-03 integration test"
      contains: "API.*Auth"
    - path: "routes/sca.js"
      provides: "Answer key stub route"
      contains: "answer-key"
    - path: "package.json"
      provides: "test:integration npm script"
      contains: "test:integration"
  key_links:
    - from: "test/sca-review.test.js"
      to: "test/helpers.js"
      via: "require('./helpers')"
      pattern: "require\\('./helpers'\\)"
    - from: "test/answer-key-gating.test.js"
      to: "routes/sca.js GET /sca/answer-key"
      via: "HTTP request to stub route"
      pattern: "sca/answer-key"
    - from: "test/api-auth.test.js"
      to: "server.js API endpoints"
      via: "HTTP requests without auth cookies"
      pattern: "api/(instructor-message|summary)"
    - from: "package.json test:integration"
      to: "test/*.test.js"
      via: "node --test glob"
      pattern: "node --test test/\\*\\.test\\.js"
---

<objective>
Add integration test coverage for the three security-critical behaviors: SCA review submission persistence, answer key role-gating, and API endpoint authentication.

Purpose: Catch regressions in core user actions and security-critical paths before they reach the classroom. These tests guard the SCA review workflow (the primary student activity), role-based access control (instructor-only content), and API authentication (endpoints that were previously open).

Output: Four new files in test/ directory (helpers + 3 test files), one modified route file (answer-key stub), one modified package.json (test:integration script). All tests run via `npm run test:integration` against a live server.
</objective>

<execution_context>
@/Users/juliegoyette/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliegoyette/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-testing/08-CONTEXT.md
@.planning/phases/08-testing/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From middleware/auth.js:
```javascript
// requireAuth redirects with 302 (NOT 401) when unauthenticated
function requireAuth(req, res, next) {
  if (req.session && req.session.user) return next();
  res.redirect('/?error=Please login first');
}
module.exports = { requireAuth, redirectIfAuthenticated };
```

From middleware/rbac.js:
```javascript
// requireRole returns 403 with rendered error page containing "Access Denied"
// WARNING: bypasses when req.securitySettings.rbac_enabled is falsy
function requireRole(allowedRoles) { /* returns middleware */ }
module.exports = { requireRole };
```

From routes/sca.js (POST /sca/findings/:id/review, lines 188-223):
```javascript
// Requires: requireAuth, requireRole(['student'])
// Body: classification (confirmed|false_positive|needs_investigation), student_notes, remediation_notes, action
// CRITICAL: action must be 'submit' for status='submitted' and submitted_at to be set
// Idempotent: UPDATE if review exists, INSERT if not
// Returns: 302 redirect to /sca/findings/:id (or JSON if Accept: application/json)
```

From routes/sca.js (end of file, line 253):
```javascript
module.exports = { router, importToVM };
```

From server.js (lines 117-130):
```javascript
// All three use requireAuth middleware
app.get('/api/instructor-message', requireAuth, (req, res) => { ... });
app.post('/api/instructor-message', requireAuth, (req, res) => { ... });
app.get('/api/summary', requireAuth, (req, res) => { ... });
```

From package.json scripts:
```json
{
  "start": "node scripts/classroom-manager.js",
  "test": "node scripts/smoke-test.js",
  "test:open": "node scripts/smoke-test.js --open"
}
```

Seed accounts (from utils/seedData.js):
- admin / admin123 (role: admin)
- prof_jones / prof123 (role: professor)
- alice_student / student123 (role: student)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure (helpers, stub route, npm script)</name>
  <files>test/helpers.js, routes/sca.js, package.json</files>
  <action>
    1. Create `test/` directory and `test/helpers.js` with these exports:
       - `BASE_URL`: `process.env.TEST_URL || 'http://localhost:3001'`
       - `REQUEST_TIMEOUT`: 5000ms
       - `CREDENTIALS` object mapping role strings to seed account credentials:
         student -> alice_student/student123, professor -> prof_jones/prof123, admin -> admin/admin123
       - `request(options)`: Promise-based HTTP client using `node:http`. Accepts `{ url, method, headers, body, timeout }`. Returns `{ statusCode, headers, body }`. Includes timeout handling (req.setTimeout + req.destroy). Does NOT follow redirects (critical for TEST-03). Replicate the pattern from scripts/smoke-test.js but as a fresh implementation -- do NOT import from smoke-test.js.
       - `getSessionCookie(response)`: Extracts `connect.sid` cookie from `set-cookie` header array. Returns the `connect.sid=xxx` string or null.
       - `loginAs(role)`: Maps role to CREDENTIALS, POSTs to `${BASE_URL}/auth/login` with form-encoded body, extracts session cookie via getSessionCookie. Throws descriptive error if login fails. Always sets Content-Type and Content-Length headers on the POST.

    2. Add answer key stub route to `routes/sca.js`:
       - Insert before the `module.exports` line (before line 253)
       - Route: `router.get('/answer-key', requireAuth, requireRole(['admin', 'professor']), (req, res) => { ... })`
       - Handler returns JSON: `res.json({ placeholder: true, message: 'Answer key coming in Phase 12' })`
       - Both `requireAuth` and `requireRole` are already imported at the top of sca.js -- no new imports needed
       - Add a comment: `// --- GET /sca/answer-key --- Stub for Phase 12 (role-gated)`

    3. Add `test:integration` script to `package.json`:
       - Add `"test:integration": "node --test test/*.test.js"` to the scripts object
       - Do NOT modify any existing scripts (especially not `test` which runs smoke-test.js)
  </action>
  <verify>
    <automated>node -e "const h = require('./test/helpers.js'); console.log(Object.keys(h)); if (!h.request || !h.getSessionCookie || !h.loginAs || !h.BASE_URL) throw new Error('Missing exports');" && grep -q 'answer-key' routes/sca.js && grep -q 'test:integration' package.json && echo "PASS"</automated>
  </verify>
  <done>test/helpers.js exports request, getSessionCookie, loginAs, and BASE_URL. routes/sca.js contains GET /sca/answer-key stub with requireAuth + requireRole(['admin', 'professor']). package.json has test:integration script pointing to node --test test/*.test.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create integration test files for TEST-01, TEST-02, and TEST-03</name>
  <files>test/sca-review.test.js, test/answer-key-gating.test.js, test/api-auth.test.js</files>
  <behavior>
    TEST-01 (test/sca-review.test.js):
    - Health check: before hook verifies server at BASE_URL/health is reachable, uses assert.ok (not process.exit)
    - Submit review: POST /sca/findings/1/review as student with classification=confirmed, action=submit, student_notes=test+note. Expect 302/303 redirect.
    - Verify persistence: GET /sca/findings/1 as same student, expect 200, body contains 'confirmed'
    - Unauthenticated submit: POST /sca/findings/1/review without cookie, expect 302 (requireAuth redirect)

    TEST-02 (test/answer-key-gating.test.js):
    - Health check: before hook (same pattern)
    - Student denied: GET /sca/answer-key as student, expect 403, body contains 'Access Denied'
    - Professor allowed: GET /sca/answer-key as professor, expect 200, body contains 'placeholder'
    - Admin allowed: GET /sca/answer-key as admin, expect 200, body contains 'placeholder'
    - Unauthenticated denied: GET /sca/answer-key without cookie, expect 302 (requireAuth redirect)

    TEST-03 (test/api-auth.test.js):
    - Health check: before hook (same pattern)
    - GET /api/instructor-message without auth: expect 302
    - POST /api/instructor-message without auth: expect 302 (send JSON body with Content-Type and Content-Length)
    - GET /api/summary without auth: expect 302
  </behavior>
  <action>
    Create all three test files using `node:test` (describe/it/before) and `node:assert` (strictEqual/ok/match). Each file requires helpers from `./helpers.js`.

    **Common pattern for all files:**
    - `const { describe, it, before } = require('node:test');`
    - `const assert = require('node:assert');`
    - `const { request, loginAs, BASE_URL } = require('./helpers');`
    - Health check in `before()` using `assert.ok()` -- NOT `process.exit(1)` (per research pitfall 6, this prevents clean test runner exit code handling). Error message: `'Server not running. Start it first: npm start'`

    **test/sca-review.test.js (TEST-01):**
    - `describe('TEST-01: SCA Review Submission', () => { ... })`
    - `before()`: health check + `studentCookie = await loginAs('student')`
    - Test "should submit a review classification and persist it":
      - POST to `${BASE_URL}/sca/findings/1/review` with form-encoded body: `classification=confirmed&action=submit&student_notes=integration+test`
      - Headers: Cookie (studentCookie), Content-Type (application/x-www-form-urlencoded), Content-Length
      - Assert statusCode is 302 or 303
      - GET `${BASE_URL}/sca/findings/1` with Cookie header
      - Assert statusCode 200
      - Assert body includes 'confirmed' (the classification value persisted and rendered)
    - Test "should reject unauthenticated review submission":
      - POST to `${BASE_URL}/sca/findings/1/review` with same body but NO Cookie header
      - Assert statusCode 302 (requireAuth redirect)

    **test/answer-key-gating.test.js (TEST-02):**
    - `describe('TEST-02: Answer Key Role-Gating', () => { ... })`
    - `before()`: health check + login as student, professor, and admin (three cookies)
    - Test "should deny student access with 403":
      - GET `${BASE_URL}/sca/answer-key` with student cookie
      - Assert statusCode 403
      - Assert body includes 'Access Denied'
    - Test "should allow professor access with 200":
      - GET `${BASE_URL}/sca/answer-key` with professor cookie
      - Assert statusCode 200
      - Assert body includes 'placeholder'
    - Test "should allow admin access with 200":
      - GET `${BASE_URL}/sca/answer-key` with admin cookie
      - Assert statusCode 200
      - Assert body includes 'placeholder'
    - Test "should redirect unauthenticated request":
      - GET `${BASE_URL}/sca/answer-key` without cookie
      - Assert statusCode 302

    **test/api-auth.test.js (TEST-03):**
    - `describe('TEST-03: API Endpoints Require Auth', () => { ... })`
    - `before()`: health check only (no login needed -- testing unauthenticated access)
    - Test "GET /api/instructor-message returns 302 without auth":
      - GET `${BASE_URL}/api/instructor-message` with no cookies
      - Assert statusCode 302
    - Test "POST /api/instructor-message returns 302 without auth":
      - POST `${BASE_URL}/api/instructor-message` with JSON body `{ "message": "test" }`, Content-Type application/json, Content-Length calculated
      - Assert statusCode 302
    - Test "GET /api/summary returns 302 without auth":
      - GET `${BASE_URL}/api/summary` with no cookies
      - Assert statusCode 302

    IMPORTANT: Do NOT use `fetch()` -- it auto-follows redirects, hiding the 302s that TEST-03 needs to verify. The node:http-based `request()` helper does not follow redirects, which is the correct behavior.
  </action>
  <verify>
    <automated>node --test test/*.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>All three test files exist and pass when run against a live server. `npm run test:integration` exits 0 with all tests passing. TEST-01 verifies review submission persistence, TEST-02 verifies role-gating (student 403, professor/admin 200), TEST-03 verifies API auth (302 without cookies).</done>
</task>

</tasks>

<verification>
1. `npm run test:integration` runs all three test files and exits 0 (requires server running on localhost:3001)
2. `node --test test/sca-review.test.js` passes -- student can submit and persist a review
3. `node --test test/answer-key-gating.test.js` passes -- student gets 403, professor/admin get 200
4. `node --test test/api-auth.test.js` passes -- all three API endpoints return 302 without auth
5. `npm test` still works (existing smoke test unchanged)
6. `curl -s http://localhost:3001/sca/answer-key` returns 302 (unauthenticated redirect to login)
</verification>

<success_criteria>
- `npm run test:integration` executes without errors and all tests pass green
- Three test files cover all three TEST requirements (TEST-01, TEST-02, TEST-03)
- Answer key stub route exists at GET /sca/answer-key with correct role-gating
- Existing `npm test` smoke test is completely unmodified and still works
- No new npm dependencies added (only built-in node:test, node:assert, node:http)
</success_criteria>

<output>
After completion, create `.planning/phases/08-testing/08-01-SUMMARY.md`
</output>
</task>
