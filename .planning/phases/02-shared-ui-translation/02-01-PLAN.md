---
phase: 02-shared-ui-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/translations/fr.json
  - config/translations/en.json
  - views/login.ejs
  - views/partials/header.ejs
  - views/error.ejs
autonomous: true
requirements: [TRAN-06, TRAN-07, TRAN-08]

must_haves:
  truths:
    - "Login page displays all labels, placeholders, buttons, and demo account instructions in French"
    - "Sidebar navigation shows all links, section titles, role badges, and lab names in French"
    - "Security status bar label is in French while badge values stay in English"
    - "Error pages display French title and guidance text per status code (404, 403, 429, 500)"
    - "Browser tab titles are in French on login, header, and error pages"
    - "All three templates declare lang=fr on the html element"
  artifacts:
    - path: "config/translations/fr.json"
      provides: "All French translation keys for nav, login, and error pages"
      contains: "nav.securityPanel"
    - path: "config/translations/en.json"
      provides: "Matching English fallback keys"
      contains: "nav.securityPanel"
    - path: "views/login.ejs"
      provides: "French login page"
      contains: "t('auth.username')"
    - path: "views/partials/header.ejs"
      provides: "French sidebar and security bar"
      contains: "t('nav.main')"
    - path: "views/error.ejs"
      provides: "French error page with status-code-based messages"
      contains: "t('errors.notFoundTitle')"
  key_links:
    - from: "views/login.ejs"
      to: "config/translations/fr.json"
      via: "t() calls for login.* and auth.* keys"
      pattern: "t\\('login\\.|t\\('auth\\."
    - from: "views/partials/header.ejs"
      to: "config/translations/fr.json"
      via: "t() calls for nav.* and common.* keys"
      pattern: "t\\('nav\\.|t\\('common\\."
    - from: "views/error.ejs"
      to: "config/translations/fr.json"
      via: "t() calls for errors.* keys with status-code-based lookup"
      pattern: "t\\('errors\\."
---

<objective>
Translate the three shared UI templates (login.ejs, header.ejs, error.ejs) to French by adding missing translation keys and replacing all hardcoded English strings with t() calls.

Purpose: Students see French from the moment they open the application -- login, navigation, and error pages are all in Quebec French. This covers the entire "shell" that wraps all authenticated content.
Output: Three fully-translated EJS templates + complete translation key coverage in both JSON files.
</objective>

<execution_context>
@/Users/juliegoyette/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliegoyette/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-ui-translation/02-CONTEXT.md
@.planning/phases/02-shared-ui-translation/02-RESEARCH.md
@.planning/phases/01-translation-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->
<!-- The t() function is already available in all EJS views via res.locals.t -->

From utils/i18n.js:
```javascript
// t(lang, key, params) - but in templates, just use t(key, params) since
// languageMiddleware binds the language:
//   res.locals.t = (key, params) => t(lang, key, params);
//
// Use <%= t('key') %> in EJS templates (HTML-escaped output)
// Use <%= t('key', { param: value }) %> for interpolation
// Missing keys fall back to English, then return the raw key string
```

From config/translations/fr.json (existing keys relevant to this plan):
```
nav.main = "Principal"
nav.administration = "Administration"
nav.teaching = "Enseignement"
nav.learning = "Apprentissage"
nav.securityLabs = "Laboratoires de securite"
nav.classes = "Cours"
nav.roleAdmin = "Administrateur"
nav.roleProf = "Professeur"
nav.roleStudent = "Etudiant(e)"
nav.auditLogs = "Journaux d'audit"
nav.backups = "Sauvegardes"
common.dashboard = "Tableau de bord"
common.login = "Connexion"
common.logout = "Deconnexion"
common.close = "Fermer"
common.error = "Erreur"
auth.username = "Nom d'utilisateur"
auth.password = "Mot de passe"
auth.invalidCredentials = "Nom d'utilisateur ou mot de passe invalide"
login.subtitle = "Plateforme pedagogique de securite applicative"
login.formTitle = "Connexion"
login.demoAccounts = "Comptes de demonstration"
login.demoAdmin = "Administrateur : admin / admin123"
login.demoProf = "Professeur : prof_jones / prof123"
login.demoStudent = "Etudiant(e) : alice_student / student123"
errors.pageNotFound = "Page non trouvee"
errors.serverError = "Une erreur s'est produite"
errors.forbidden = "Acces interdit"
errors.goHome = "Aller au tableau de bord"
```

Keys that DO NOT exist yet and must be added (see Task 1):
```
nav.securityPanel, nav.mfaSetup, nav.staticAnalysis, nav.dynamicAnalysis,
nav.vulnManagement, nav.pentestLab (overwrite), nav.myClasses, nav.myEnrollments,
nav.appSubtitle, nav.securityStatus, nav.defaultTitle,
login.footerText, login.loginFailed,
errors.notFoundTitle, errors.forbiddenTitle, errors.tooManyAttemptsTitle,
errors.serverErrorTitle, errors.notFoundGuidance, errors.forbiddenGuidance,
errors.tooManyAttemptsGuidance, errors.serverErrorGuidance, errors.backToDashboard
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing translation keys to fr.json and en.json</name>
  <files>config/translations/fr.json, config/translations/en.json</files>
  <action>
Add the following keys to BOTH fr.json and en.json. Place them in their respective namespace objects (nav.*, login.*, errors.*). For keys that already exist with different values (like nav.pentestLab), overwrite the value.

**nav namespace additions (add alongside existing nav keys):**
- nav.securityPanel: FR "Panneau de securite" / EN "Security Panel"
- nav.mfaSetup: FR "Configuration AMF" / EN "MFA Setup"
- nav.staticAnalysis: FR "Analyse statique" / EN "Static Analysis"
- nav.dynamicAnalysis: FR "Analyse dynamique" / EN "Dynamic Analysis"
- nav.vulnManagement: FR "Gestion des vulnerabilites" / EN "Vuln Management"
- nav.pentestLab: FR "Test d'intrusion" / EN "Pentest Lab" (OVERWRITE existing value -- user wants "Test d'intrusion" not "Labo Pentest")
- nav.myClasses: FR "Mes cours" / EN "My Classes"
- nav.myEnrollments: FR "Mes inscriptions" / EN "My Enrollments"
- nav.appSubtitle: FR "Securite applicative" / EN "Application Security"
- nav.securityStatus: FR "Etat de securite :" / EN "Security Status:"
- nav.defaultTitle: FR "HEC Montreal - Securite applicative" / EN "HEC Montreal - Application Security"

**login namespace additions:**
- login.footerText: FR "Plateforme pedagogique de demonstration en securite applicative" / EN "Educational application security demonstration platform"
- login.loginFailed: FR "Echec de connexion" / EN "Login Failed"

**errors namespace additions (add alongside existing errors keys):**
- errors.notFoundTitle: FR "Page introuvable" / EN "Page not found"
- errors.forbiddenTitle: FR "Acces refuse" / EN "Access denied"
- errors.tooManyAttemptsTitle: FR "Trop de tentatives" / EN "Too many attempts"
- errors.serverErrorTitle: FR "Erreur du serveur" / EN "Server error"
- errors.notFoundGuidance: FR "La page que vous recherchez n'existe pas ou a ete deplacee." / EN "The page you are looking for does not exist or has been moved."
- errors.forbiddenGuidance: FR "Vous n'avez pas la permission d'acceder a cette page." / EN "You do not have permission to access this page."
- errors.tooManyAttemptsGuidance: FR "Vous avez depasse le nombre maximal de tentatives de connexion. Veuillez reessayer dans quelques minutes." / EN "You have exceeded the maximum number of login attempts. Please try again in a few minutes."
- errors.serverErrorGuidance: FR "Une erreur inattendue s'est produite. Veuillez reessayer plus tard." / EN "An unexpected error occurred. Please try again later."
- errors.backToDashboard: FR "Retour au tableau de bord" / EN "Back to Dashboard"

IMPORTANT: Use proper French accents and cedillas in fr.json (e.g., "Securite" should be "Sécurité", "Acces refuse" should be "Accès refusé"). The research doc stripped accents for plain-text display, but the actual JSON must have proper Unicode characters. Match the accent style already used in the existing fr.json entries.

Do NOT remove or rename any existing keys. Only add new keys and overwrite nav.pentestLab.
  </action>
  <verify>
    <automated>node -e "const fr=require('./config/translations/fr.json'); const en=require('./config/translations/en.json'); const keys=['nav.securityPanel','nav.mfaSetup','nav.staticAnalysis','nav.dynamicAnalysis','nav.vulnManagement','nav.myClasses','nav.myEnrollments','nav.appSubtitle','nav.securityStatus','nav.defaultTitle','login.footerText','login.loginFailed','errors.notFoundTitle','errors.forbiddenTitle','errors.tooManyAttemptsTitle','errors.serverErrorTitle','errors.notFoundGuidance','errors.forbiddenGuidance','errors.tooManyAttemptsGuidance','errors.serverErrorGuidance','errors.backToDashboard']; let ok=true; for(const k of keys){const [ns,key]=k.split('.'); if(!fr[ns]||!fr[ns][key]){console.error('MISSING fr:',k);ok=false;} if(!en[ns]||!en[ns][key]){console.error('MISSING en:',k);ok=false;}} if(fr.nav.pentestLab!=='Test d\\'intrusion'){console.error('nav.pentestLab not overwritten');ok=false;} if(ok)console.log('ALL KEYS PRESENT'); process.exit(ok?0:1);"</automated>
  </verify>
  <done>All 21 new translation keys exist in both fr.json and en.json with proper French accents. nav.pentestLab value is overwritten to "Test d'intrusion". No existing keys removed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire login.ejs with t() calls and set lang="fr"</name>
  <files>views/login.ejs</files>
  <action>
Replace all hardcoded English strings in views/login.ejs with t() calls. The t() function is already available via res.locals.t (languageMiddleware runs before all routes including the login GET route in server.js line 231).

Specific replacements:

1. **HTML lang attribute:** Change `<html lang="en">` to `<html lang="fr">`

2. **Title tag:** Change `<title>Login - HEC Montreal Application Security</title>` to:
   `<title><%= t('login.formTitle') %> - HEC Montreal <%= t('nav.appSubtitle') %></title>`

3. **Subtitle:** Change `<p class="subtitle">Application Security Learning Platform</p>` to:
   `<p class="subtitle"><%= t('login.subtitle') %></p>`

4. **Error alert title:** Change `<div class="alert-danger-title">Login Failed</div>` to:
   `<div class="alert-danger-title"><%= t('login.loginFailed') %></div>`

5. **Error alert message:** Change `<div class="alert-danger-message"><%= error %></div>` to:
   `<div class="alert-danger-message"><%= t('auth.invalidCredentials') %></div>`
   (Per research recommendation: ignore server-provided English string, show translated message since login only has credential errors)

6. **Username label:** Change `<label for="username">Username</label>` to:
   `<label for="username"><%= t('auth.username') %></label>`

7. **Password label:** Change `<label for="password">Password</label>` to:
   `<label for="password"><%= t('auth.password') %></label>`

8. **Login button:** Change `<button type="submit" class="btn">Login</button>` to:
   `<button type="submit" class="btn"><%= t('common.login') %></button>`

9. **Demo accounts heading:** Change `<h3>Default Accounts:</h3>` to:
   `<h3><%= t('login.demoAccounts') %> :</h3>`

10. **Demo admin:** Change `<p><strong>Admin:</strong> admin / admin123</p>` to:
    `<p><strong><%= t('login.demoAdmin') %></strong></p>`

11. **Demo professor:** Change `<p><strong>Professor:</strong> prof_jones / prof123</p>` to:
    `<p><strong><%= t('login.demoProf') %></strong></p>`

12. **Demo student:** Change `<p><strong>Student:</strong> alice_student / student123</p>` to:
    `<p><strong><%= t('login.demoStudent') %></strong></p>`

13. **Footer text:** Change `Educational application security demonstration platform` to:
    `<%= t('login.footerText') %>`

Do NOT modify any CSS styles or the form action URL. Do NOT change the "HEC Montreal" text in the h1 (it's a proper noun). Keep the HEC Montreal logo img tag unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('views/login.ejs','utf8'); const checks=[['lang=\"fr\"','html lang=fr'],['t(\\'login.formTitle\\')','title tag'],['t(\\'login.subtitle\\')','subtitle'],['t(\\'login.loginFailed\\')','error title'],['t(\\'auth.username\\')','username label'],['t(\\'auth.password\\')','password label'],['t(\\'common.login\\')','login button'],['t(\\'login.demoAccounts\\')','demo heading'],['t(\\'login.demoAdmin\\')','demo admin'],['t(\\'login.demoProf\\')','demo prof'],['t(\\'login.demoStudent\\')','demo student'],['t(\\'login.footerText\\')','footer']]; let ok=true; for(const [pattern,name] of checks){if(!html.includes(pattern)){console.error('MISSING:',name,'->',pattern);ok=false;}} if(html.includes('lang=\"en\"')){console.error('Still has lang=en');ok=false;} if(ok)console.log('LOGIN.EJS VERIFIED'); process.exit(ok?0:1);"</automated>
  </verify>
  <done>Login page has zero hardcoded English strings. All user-visible text uses t() calls. HTML declares lang="fr". Browser tab title is in French.</done>
</task>

<task type="auto">
  <name>Task 3: Wire header.ejs and error.ejs with t() calls and set lang="fr"</name>
  <files>views/partials/header.ejs, views/error.ejs</files>
  <action>
**PART A: header.ejs (views/partials/header.ejs)**

Replace all hardcoded English strings with t() calls. The t() function is available in all views.

1. **HTML lang:** Change `<html lang="en">` to `<html lang="fr">`

2. **Title tag fallback:** Change the title line to:
   `<title><%= typeof title !== 'undefined' ? title : t('nav.defaultTitle') %></title>`

3. **Sidebar subtitle:** Change `<div class="sidebar-logo-subtitle">Application Security</div>` to:
   `<div class="sidebar-logo-subtitle"><%= t('nav.appSubtitle') %></div>`

4. **Instructor broadcast dismiss button:** Change the `Dismiss` text in the button to:
   Replace `>Dismiss</button>` with `><%= t('common.close') %></button>`

5. **Main section title:** Change `<div class="nav-section-title">Main</div>` to:
   `<div class="nav-section-title"><%= t('nav.main') %></div>`

6. **Dashboard link:** Change `<span>Dashboard</span>` to `<span><%= t('common.dashboard') %></span>`

7. **Classes link:** Change `<span>Classes</span>` to `<span><%= t('nav.classes') %></span>`

8. **Administration section title:** Change `<div class="nav-section-title">Administration</div>` to:
   `<div class="nav-section-title"><%= t('nav.administration') %></div>`

9. **Security Panel link:** Change `<span>Security Panel</span>` to `<span><%= t('nav.securityPanel') %></span>`

10. **Audit Logs link:** Change `<span>Audit Logs</span>` to `<span><%= t('nav.auditLogs') %></span>`

11. **MFA Setup link:** Change `<span>MFA Setup</span>` to `<span><%= t('nav.mfaSetup') %></span>`

12. **Backups link:** Change `<span>Backups</span>` to `<span><%= t('nav.backups') %></span>`

13. **Teaching section title:** Change `<div class="nav-section-title">Teaching</div>` to:
    `<div class="nav-section-title"><%= t('nav.teaching') %></div>`

14. **My Classes link:** Change `<span>My Classes</span>` to `<span><%= t('nav.myClasses') %></span>`

15. **Learning section title:** Change `<div class="nav-section-title">Learning</div>` to:
    `<div class="nav-section-title"><%= t('nav.learning') %></div>`

16. **My Enrollments link:** Change `<span>My Enrollments</span>` to `<span><%= t('nav.myEnrollments') %></span>`

17. **Security Labs section title:** Change `<div class="nav-section-title">Security Labs</div>` to:
    `<div class="nav-section-title"><%= t('nav.securityLabs') %></div>`

18. **Static Analysis link:** Change `<span>Static Analysis</span>` to `<span><%= t('nav.staticAnalysis') %></span>`

19. **Dynamic Analysis link:** Change `<span>Dynamic Analysis</span>` to `<span><%= t('nav.dynamicAnalysis') %></span>`

20. **Vuln Management link:** Change `<span>Vuln Management</span>` to `<span><%= t('nav.vulnManagement') %></span>`

21. **Pentest Lab link:** Change `<span>Pentest Lab</span>` to `<span><%= t('nav.pentestLab') %></span>`

22. **User role display:** Change `<div class="user-role"><%= user.role %></div>` to use a role lookup map:
    ```ejs
    <div class="user-role"><%= ({admin: t('nav.roleAdmin'), professor: t('nav.roleProf'), student: t('nav.roleStudent')})[user.role] || user.role %></div>
    ```

23. **Logout button:** Change `<a href="/auth/logout" class="logout-btn">Logout</a>` to:
    `<a href="/auth/logout" class="logout-btn"><%= t('common.logout') %></a>`

24. **Security Status label:** Change `<span class="security-label">Security Status:</span>` to:
    `<span class="security-label"><%= t('nav.securityStatus') %></span>`

25. **Security badges:** Keep the badge VALUE text in English per user decision (MFA: ON/OFF, RBAC: ON/OFF, Passwords: Encrypted/Plaintext, etc.) -- do NOT translate these. They are industry-standard terms.

**PART B: error.ejs (views/error.ejs)**

Replace hardcoded English strings using status-code-based translation lookup pattern (from Research Pattern 2). Do NOT modify server.js or any server-side files.

1. **HTML lang:** Change `<html lang="en">` to `<html lang="fr">`

2. **Title tag:** Change `<title>Error - University Class Management</title>` to:
   `<title><%= t('common.error') %> - HEC Montr&eacute;al <%= t('nav.appSubtitle') %></title>`

3. **Error title (h1):** Replace `<h1><%= message %></h1>` with a status-code-based lookup. Add a scriptlet block before the h1 that maps status codes to translation keys:
   ```ejs
   <%
     var statusCode = (error && error.status) ? error.status : 500;
     var titleMap = {
       404: t('errors.notFoundTitle'),
       403: t('errors.forbiddenTitle'),
       429: t('errors.tooManyAttemptsTitle'),
     };
     var guidanceMap = {
       404: t('errors.notFoundGuidance'),
       403: t('errors.forbiddenGuidance'),
       429: t('errors.tooManyAttemptsGuidance'),
     };
     var errorTitle = titleMap[statusCode] || t('errors.serverErrorTitle');
     var errorGuidance = guidanceMap[statusCode] || t('errors.serverErrorGuidance');
   %>
   <h1><%= errorTitle %></h1>
   ```

4. **Error details:** Replace the `<% if (error && error.details) { %>` block with the translated guidance:
   ```ejs
   <p class="error-message"><%= errorGuidance %></p>
   ```
   This replaces the English `error.details` with French guidance. The status code number still displays above it.

5. **Stack trace label:** Keep "Stack Trace (Development Mode):" in English per user decision (developer-facing).

6. **Back button:** Change `<a href="/dashboard" class="btn">&larr; Back to Dashboard</a>` to:
   `<a href="/dashboard" class="btn">&larr; <%= t('errors.backToDashboard') %></a>`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('views/partials/header.ejs','utf8'); const e=fs.readFileSync('views/error.ejs','utf8'); let ok=true; const hChecks=[['lang=\"fr\"','header lang'],['t(\\'nav.main\\')','nav main'],['t(\\'common.dashboard\\')','dashboard'],['t(\\'nav.securityPanel\\')','security panel'],['t(\\'nav.mfaSetup\\')','mfa setup'],['t(\\'nav.staticAnalysis\\')','static analysis'],['t(\\'nav.dynamicAnalysis\\')','dynamic analysis'],['t(\\'nav.vulnManagement\\')','vuln mgmt'],['t(\\'nav.pentestLab\\')','pentest'],['t(\\'nav.myClasses\\')','my classes'],['t(\\'nav.myEnrollments\\')','my enrollments'],['t(\\'common.logout\\')','logout'],['t(\\'nav.securityStatus\\')','security status'],['t(\\'nav.appSubtitle\\')','app subtitle'],['t(\\'nav.roleAdmin\\')','role admin'],['t(\\'common.close\\')','dismiss btn']]; for(const [p,n] of hChecks){if(!h.includes(p)){console.error('header MISSING:',n);ok=false;}} if(h.includes('lang=\"en\"')){console.error('header still has lang=en');ok=false;} const eChecks=[['lang=\"fr\"','error lang'],['t(\\'errors.notFoundTitle\\')','404 title'],['t(\\'errors.forbiddenTitle\\')','403 title'],['t(\\'errors.tooManyAttemptsTitle\\')','429 title'],['t(\\'errors.serverErrorTitle\\')','500 title'],['t(\\'errors.backToDashboard\\')','back btn'],['t(\\'nav.appSubtitle\\')','error title tag']]; for(const [p,n] of eChecks){if(!e.includes(p)){console.error('error MISSING:',n);ok=false;}} if(e.includes('lang=\"en\"')){console.error('error still has lang=en');ok=false;} if(ok)console.log('HEADER+ERROR VERIFIED'); process.exit(ok?0:1);"</automated>
  </verify>
  <done>Header sidebar shows all nav links, section titles, role badges, and lab names in French. Security badges stay English. Error page shows French title and guidance per status code. Both templates declare lang="fr".</done>
</task>

</tasks>

<verification>
After all tasks complete, run the application and verify:

1. **Login page (unauthenticated):** Visit http://localhost:3000/ -- all text should be in French. Browser tab shows "Connexion - HEC Montreal Securite applicative". Demo accounts show French role labels.

2. **Sidebar (authenticated):** Log in as admin -- sidebar section titles, nav links, admin links, security labs, user role, and logout button should all be in French. Security badge VALUES stay English ("MFA: ON", "RBAC: ON", etc.) but the "Security Status:" label is in French.

3. **Error page (404):** Visit http://localhost:3000/nonexistent -- should show "Page introuvable" with French guidance text and "Retour au tableau de bord" button.

4. **No hardcoded English in templates:** Verify no English strings remain in the three template files (except: "HEC Montreal" proper noun, security badge values, stack trace label, emoji icons).
</verification>

<success_criteria>
- Zero hardcoded English user-facing strings in login.ejs, header.ejs, or error.ejs (excluding proper nouns, security badge values, stack trace label)
- All three templates have `<html lang="fr">`
- All 21 new translation keys present in both fr.json and en.json with proper French accents
- nav.pentestLab overwritten to "Test d'intrusion" in fr.json
- Login page renders entirely in French including demo accounts
- Sidebar renders all sections in French for all three roles (admin, professor, student)
- Error page shows status-code-specific French titles and guidance
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-ui-translation/02-01-SUMMARY.md`
</output>
