---
phase: 14-code-quality
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - server.js
  - config/database.js
  - config/security.js
  - middleware/audit.js
  - middleware/auth.js
  - middleware/rateLimiter.js
  - middleware/rbac.js
  - routes/admin.js
  - routes/auth.js
  - routes/classes.js
  - routes/dashboard.js
  - routes/dast.js
  - routes/sca.js
  - routes/sessions.js
  - routes/vm.js
  - scripts/classroom-manager.js
  - scripts/classroom-stop.js
  - scripts/setup.js
  - scripts/smoke-test.js
  - test/helpers.js
  - test/answer-key-gating.test.js
  - test/api-auth.test.js
  - test/instructor-tools.test.js
  - test/sca-review.test.js
  - utils/backupManager.js
  - utils/encryption.js
  - utils/i18n.js
  - utils/passwordHash.js
  - utils/seedData.js
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "Running `npm run lint` reports zero errors and zero warnings"
    - "Running `npm run format:check` reports all JS files are already formatted (exit code 0)"
    - "No dead code or unused variables remain (verified by ESLint no-unused-vars rule)"
    - "All 12 intentional SCA vulnerabilities are preserved exactly as-is in the codebase"
    - "The SQL pattern-matching DB adapter (config/database.js) functions identically"
    - "`npm test` smoke test passes after all changes"
    - "Pentest module (routes/pentest.js) is untouched"
  artifacts:
    - path: "server.js"
      provides: "Formatted, lint-clean main server file with 12 intentional vulnerabilities preserved"
    - path: "config/database.js"
      provides: "Formatted, lint-clean DB adapter with SQL pattern-matching logic preserved"
    - path: "utils/encryption.js"
      provides: "Formatted, lint-clean encryption utils with hardcoded key vulnerability preserved"
  key_links:
    - from: "npm run lint"
      to: "all JS files"
      via: "ESLint 9 with eslint.config.js"
      pattern: "0 errors"
    - from: "npm run format:check"
      to: "all JS files"
      via: "Prettier 3 with .prettierrc"
      pattern: "All matched files"
    - from: "npm test"
      to: "scripts/smoke-test.js"
      via: "node scripts/smoke-test.js"
      pattern: "PASSED"
---

<objective>
Fix all ESLint errors, format all JS files with Prettier, and remove dead code -- while preserving every intentional vulnerability and the DB adapter.

Purpose: Achieve zero lint errors and consistent formatting across the entire codebase (QUAL-02, QUAL-03) without breaking any existing functionality (QUAL-04).

Output: All ~30 JS files pass ESLint and Prettier cleanly, dead code removed, smoke test green.
</objective>

<execution_context>
@/Users/juliegoyette/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliegoyette/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-code-quality/14-CONTEXT.md
@.planning/phases/14-code-quality/14-RESEARCH.md
@.planning/phases/14-code-quality/14-01-SUMMARY.md
@eslint.config.js
@.prettierrc
@SECURITY-BOUNDARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ESLint errors and remove dead code</name>
  <files>server.js, config/database.js, config/security.js, middleware/audit.js, middleware/auth.js, middleware/rateLimiter.js, middleware/rbac.js, routes/admin.js, routes/auth.js, routes/classes.js, routes/dashboard.js, routes/dast.js, routes/sca.js, routes/sessions.js, routes/vm.js, scripts/classroom-manager.js, scripts/classroom-stop.js, scripts/setup.js, scripts/smoke-test.js, test/helpers.js, test/answer-key-gating.test.js, test/api-auth.test.js, test/instructor-tools.test.js, test/sca-review.test.js, utils/backupManager.js, utils/encryption.js, utils/i18n.js, utils/passwordHash.js, utils/seedData.js</files>
  <action>
WORKFLOW: Run `npm run lint` first to see the actual errors. Then fix them iteratively.

**Step 1 -- Baseline:**
Run `npm run lint` and capture all errors. Categorize each error:
- `no-unused-vars`: Determine if truly unused or needed. If truly unused, remove the variable/import. If it is an Express middleware signature parameter (e.g., `next` in error handlers, `req` in routes), prefix with underscore (`_next`, `_req`).
- `no-unreachable`: Remove unreachable code after return/throw statements.
- Other `eslint:recommended` rules: Fix case-by-case.

**Step 2 -- Dead code removal (QUAL-03):**
- Remove any variables/functions flagged by `no-unused-vars` that are genuinely dead (not used elsewhere).
- Remove commented-out code blocks (git history preserves them). Look for multi-line `// ...` or `/* ... */` blocks that contain old code, NOT documentation comments.
- Do NOT touch `routes/pentest.js` -- user decision: leave pentest module untouched, Phase 16 replaces it entirely. If pentest.js has lint errors, add it to the ESLint ignores in eslint.config.js instead.

**Step 3 -- Intentional vulnerability preservation (QUAL-04 -- CRITICAL):**
Reference SECURITY-BOUNDARY.md for the 12 intentional vulnerabilities. Per the research, most of these are security logic issues (hardcoded secrets, missing checks) that will NOT trigger `eslint:recommended` rules. Only add `// eslint-disable-next-line {rule}` comments if a specific vulnerability line actually triggers a lint error. Do NOT preemptively add eslint-disable comments.

For `config/database.js` (SQL pattern-matching adapter): If any lines trigger lint errors, use inline `// eslint-disable-next-line {rule}` on those specific lines. Do not restructure the adapter logic.

**Step 4 -- Verify after each batch of fixes:**
Run `npm test` after fixing each file group (routes, middleware, utils, etc.) to catch regressions immediately. If a test fails, revert the last change and investigate.

**Step 5 -- Final lint check:**
Run `npm run lint` and confirm zero errors and zero warnings. If any remain, fix them. Repeat until clean.
  </action>
  <verify>
    <automated>cd /Users/juliegoyette/Desktop/Universisty/university-class-management && npm run lint 2>&1; echo "LINT_EXIT: $?"; npm test 2>&1 | tail -5; echo "TEST_EXIT: $?"</automated>
  </verify>
  <done>`npm run lint` exits with code 0 and reports zero errors/warnings. `npm test` smoke test passes. routes/pentest.js is unchanged. All 12 intentional vulnerabilities are preserved (verified by smoke test passing).</done>
</task>

<task type="auto">
  <name>Task 2: Format codebase with Prettier and verify clean state</name>
  <files>server.js, config/database.js, config/security.js, middleware/audit.js, middleware/auth.js, middleware/rateLimiter.js, middleware/rbac.js, routes/admin.js, routes/auth.js, routes/classes.js, routes/dashboard.js, routes/dast.js, routes/sca.js, routes/sessions.js, routes/vm.js, scripts/classroom-manager.js, scripts/classroom-stop.js, scripts/setup.js, scripts/smoke-test.js, test/helpers.js, test/answer-key-gating.test.js, test/api-auth.test.js, test/instructor-tools.test.js, test/sca-review.test.js, utils/backupManager.js, utils/encryption.js, utils/i18n.js, utils/passwordHash.js, utils/seedData.js</files>
  <action>
**Step 1 -- Run Prettier:**
```bash
npm run format
```
This applies Prettier formatting to all JS files (EJS excluded via .prettierignore). Prettier only changes whitespace, quotes, semicolons, and line breaks -- it does NOT change code semantics, so it is safe for intentional vulnerabilities.

**Step 2 -- Verify formatting is stable:**
```bash
npm run format:check
```
This must exit with code 0, meaning all files are already formatted (no changes needed on a second pass).

**Step 3 -- Run smoke test after formatting:**
```bash
npm test
```
Prettier is whitespace-only so the smoke test should pass. If it fails, investigate -- Prettier should not break functionality.

**Step 4 -- Run lint after formatting:**
```bash
npm run lint
```
Confirm still zero errors. The `eslint-config-prettier` in the ESLint config prevents any lint/format conflicts, but verify anyway.

**Step 5 -- Run integration tests:**
```bash
npm run test:integration
```
Full integration test suite must pass to confirm no regressions.

**Step 6 -- Final comprehensive check:**
Run the complete validation sequence:
```bash
npm run lint && npm run format:check && npm test
```
All three must pass with zero errors.
  </action>
  <verify>
    <automated>cd /Users/juliegoyette/Desktop/Universisty/university-class-management && npm run lint 2>&1 | tail -3 && npm run format:check 2>&1 | tail -3 && npm test 2>&1 | tail -5; echo "FINAL_EXIT: $?"</automated>
  </verify>
  <done>`npm run lint` reports zero errors. `npm run format:check` exits 0 (all files formatted). `npm test` passes. `npm run test:integration` passes. Codebase is consistently formatted with Prettier 3 and lint-clean with ESLint 9.</done>
</task>

</tasks>

<verification>
- `npm run lint` exits 0 with zero errors and zero warnings
- `npm run format:check` exits 0 (all JS files already formatted)
- `npm test` passes (smoke test -- all 13 ports, auth journey, answer key gating)
- `npm run test:integration` passes (SCA review, answer key, API auth, instructor tools)
- `routes/pentest.js` has no modifications (diff shows no changes)
- SECURITY-BOUNDARY.md lists 12 vulnerabilities and all are still present in code
</verification>

<success_criteria>
- Zero ESLint errors and warnings across the entire codebase
- All JS files pass Prettier formatting check (consistent style)
- Dead code and unused variables removed (ESLint-driven)
- All 12 intentional vulnerabilities preserved unchanged
- SQL pattern-matching DB adapter functions identically
- Pentest module untouched
- Smoke test and integration tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-quality/14-02-SUMMARY.md`
</output>
