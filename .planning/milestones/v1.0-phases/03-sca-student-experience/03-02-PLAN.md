---
phase: 03-sca-student-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - views/sca/student-lab.ejs
  - views/sca/finding-detail.ejs
autonomous: true
requirements:
  - TRAN-02
  - TRAN-03
  - SCAC-02
  - SCAC-03

must_haves:
  truths:
    - "Student-lab page displays all headings, labels, button text, and status badges in French via t() calls"
    - "Student-lab page shows a dismissible blue intro banner explaining the exercise approach"
    - "Student-lab page shows color-coded difficulty badges (Facile/Moyen/Avance) on each finding card"
    - "Student-lab page findings are rendered in difficulty order (Facile first) as received from the route handler"
    - "AJAX save/submit feedback messages appear in French"
    - "Finding-detail page displays all headings, labels, button text, and dropdown options in French via t() calls"
    - "Finding-detail page shows a difficulty badge matching the finding's difficulty level"
    - "Finding-detail page has a collapsible 'Besoin d aide ?' section with per-finding hint questions"
    - "Classification dropdown shows French labels: Vrai positif, Faux positif, Necessite une investigation"
  artifacts:
    - path: "views/sca/student-lab.ejs"
      provides: "Fully French student lab with intro banner, difficulty badges, translated AJAX feedback"
      min_lines: 120
    - path: "views/sca/finding-detail.ejs"
      provides: "Fully French finding detail with difficulty badge, collapsible hints, translated form"
      min_lines: 120
  key_links:
    - from: "views/sca/student-lab.ejs"
      to: "config/translations/fr.json"
      via: "t() calls"
      pattern: "t\\('sca\\.studentLab"
    - from: "views/sca/student-lab.ejs"
      to: "localStorage"
      via: "banner dismissal"
      pattern: "localStorage.*sca-intro"
    - from: "views/sca/finding-detail.ejs"
      to: "config/translations/fr.json"
      via: "t() calls for hints"
      pattern: "t\\('sca\\.findings.*hint"
    - from: "views/sca/finding-detail.ejs"
      to: "config/translations/fr.json"
      via: "t() calls for labels"
      pattern: "t\\('sca\\.findingDetail"
---

<objective>
Wire the student-lab and finding-detail EJS templates with French translations, guided intro banner, difficulty badges, AJAX feedback, and contextual hints.

Purpose: Transforms the two student-facing SCA views from hardcoded English into a fully French experience with pedagogical scaffolding -- the core deliverable students will interact with in class.
Output: Fully translated student-lab.ejs and finding-detail.ejs with all UI features.
</objective>

<execution_context>
@/Users/juliegoyette/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliegoyette/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sca-student-experience/03-CONTEXT.md
@.planning/phases/03-sca-student-experience/03-RESEARCH.md
@.planning/phases/03-sca-student-experience/03-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan depends on -->

From routes/sca.js (after Plan 01 modifications):
- Student GET /sca passes: `{ title, findings, reviewMap, submitted, total }`
  - `findings` is an array of localized objects with `.difficulty` field ('easy'|'medium'|'advanced')
  - Findings are pre-sorted: easy first, then medium, then advanced
  - Each finding has: id, title (French), description (French, enriched), remediation (French), severity, cwe, category, tool, file_path, line_number, code_snippet, difficulty
- Student GET /sca/findings/:id passes: `{ title, finding, myReview, allReviews, vmEntry }`
  - `finding` is a localized object with `.difficulty` field

From config/translations/fr.json (relevant keys for templates):
```
sca.studentLab.title          = "Laboratoire d'analyse de code statique"
sca.studentLab.subtitle       = "Examinez chaque constat..."
sca.studentLab.findingsSubmitted = "constats soumis"
sca.studentLab.complete        = "termine"
sca.studentLab.startReview     = "Commencer l'analyse"
sca.studentLab.continueReview  = "Continuer"
sca.studentLab.viewEdit        = "Voir / Modifier"
sca.studentLab.draftSaved      = "Brouillon enregistre"
sca.studentLab.submitted       = "Soumis"

sca.findingDetail.backToLab    = "Laboratoire ACS"
sca.findingDetail.location     = "Emplacement"
sca.findingDetail.codeSnippet  = "Extrait de code"
sca.findingDetail.description  = "Description"
sca.findingDetail.remediationGuidance = "Conseils de remediation"
sca.findingDetail.yourReview   = "Votre analyse"
sca.findingDetail.submittedOn  = "Soumis le"
sca.findingDetail.classification = "Classification"
sca.findingDetail.selectOption = "-- choisir --"
sca.findingDetail.yourNotes    = "Vos notes"
sca.findingDetail.proposedRemediation = "Remediation proposee"
sca.findingDetail.saveDraft    = "Enregistrer le brouillon"
sca.findingDetail.submit       = "Soumettre"
sca.findingDetail.studentReviews = "Analyses des etudiants"
sca.findingDetail.vulnerabilityManager = "Gestionnaire de vulnerabilites"
sca.findingDetail.importedToVM = "Importe dans le GV comme"
sca.findingDetail.notImported  = "Ce constat n'a pas encore ete importe..."
sca.findingDetail.pushToVM     = "Envoyer au GV"
sca.findingDetail.references   = "References"
sca.findingDetail.tool         = "Outil"

sca.common.truePositive        = "Vrai positif"
sca.common.truePositiveDesc    = "Vrai positif (vulnerabilite confirmee)"
sca.common.falsePositive       = "Faux positif"
sca.common.needsInvestigation  = "Necessite une investigation"
sca.common.saving              = "Enregistrement..."
sca.common.savedDraft          = "Brouillon enregistre."
sca.common.submittedSuccess    = "Soumis !"
sca.common.errorSaving         = "Erreur lors de l'enregistrement."
sca.common.networkError        = "Erreur reseau -- veuillez reessayer."

sca.guided.introBannerTitle    = "Comment aborder cet exercice"
sca.guided.introBannerText     = "Pour chaque constat, lisez le code..."
sca.guided.dismiss             = "Compris"

sca.difficulty.easy            = "Facile"
sca.difficulty.medium          = "Moyen"
sca.difficulty.advanced        = "Avance"

sca.findings.{id}.hint1        = "..." (guiding question)
sca.findings.{id}.hint2        = "..." (guiding question)
sca.findings.{id}.hint3        = "..." (optional, guiding question)
```

From views/partials/header.ejs (established t() pattern):
```ejs
<%= t('nav.scaLab') %>   <!-- Example: how t() is called in EJS -->
```

From views/sca/student-lab.ejs (existing JS patterns):
```javascript
function toggleForm(id) { ... }           // expand/collapse finding forms
async function saveReview(event, findingId) { ... }  // AJAX POST with FormData
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire student-lab.ejs with t() calls, intro banner, difficulty badges, and French AJAX feedback</name>
  <files>views/sca/student-lab.ejs</files>
  <action>
Rewrite student-lab.ejs to replace ALL hardcoded English strings with t() calls and add three new features. The template structure and styling remain the same -- this is a translation + feature addition, not a redesign.

**A. Replace all hardcoded English with t() calls (TRAN-02):**

| Current English | Replace with |
|----------------|--------------|
| "Static Code Analysis Lab" | `<%= t('sca.studentLab.title') %>` |
| "Review each finding, classify it..." | `<%= t('sca.studentLab.subtitle') %>` |
| "findings submitted" | `<%= t('sca.studentLab.findingsSubmitted') %>` |
| "% complete" | `<%= t('sca.studentLab.complete') %>` |
| "Submitted" (badge) | `<%= t('sca.studentLab.submitted') %>` |
| "Draft saved" (badge) | `<%= t('sca.studentLab.draftSaved') %>` |
| "View / Edit" button | `<%= t('sca.studentLab.viewEdit') %>` |
| "Continue" button | `<%= t('sca.studentLab.continueReview') %>` |
| "Start Review" button | `<%= t('sca.studentLab.startReview') %>` |
| "Code snippet:" | `<%= t('sca.findingDetail.codeSnippet') %>` |
| "Classification" label | `<%= t('sca.findingDetail.classification') %>` |
| "-- select --" | `<%= t('sca.findingDetail.selectOption') %>` |
| "True Positive (confirmed vulnerability)" | `<%= t('sca.common.truePositiveDesc') %>` |
| "False Positive" | `<%= t('sca.common.falsePositive') %>` |
| "Needs Further Investigation" | `<%= t('sca.common.needsInvestigation') %>` |
| "Your Analysis Notes" | `<%= t('sca.findingDetail.yourNotes') %>` |
| "Explain why you classified..." placeholder | Use a translated placeholder, e.g. `<%= t('sca.findingDetail.yourNotes') %>...` |
| "Proposed Remediation" | `<%= t('sca.findingDetail.proposedRemediation') %>` |
| "How would you fix this?" placeholder | Use translated text |
| "Save Draft" button | `<%= t('sca.findingDetail.saveDraft') %>` |
| "Submit" button | `<%= t('sca.findingDetail.submit') %>` |
| "Cancel" button | `<%= t('common.cancel') %>` |

**B. Add guided intro banner (SCAC-02):**
Insert between the page-header div and the progress card div. Per CONTEXT.md locked decision:
- Full-width blue info card: background #e8f0ff, HEC navy text (#002855)
- Only shown for students (check `user.role === 'student'` -- user is available via res.locals)
- Contains: `t('sca.guided.introBannerTitle')` as heading, `t('sca.guided.introBannerText')` as body
- Dismiss button labeled `t('sca.guided.dismiss')` ("Compris")
- Dismissal persisted via localStorage key `sca-intro-dismissed`
- On page load, check localStorage -- if dismissed, hide banner immediately
- Structure:
  ```html
  <div id="sca-intro-banner" class="card" style="background:#e8f0ff; border:1px solid #b8d4f0; margin-bottom:1.5rem;">
    <strong style="color:#002855; font-size:1.05rem;"><%= t('sca.guided.introBannerTitle') %></strong>
    <p style="color:#002855; margin:0.5rem 0;"><%= t('sca.guided.introBannerText') %></p>
    <button onclick="dismissBanner()" style="background:#002855; color:#fff; border:none; border-radius:4px; padding:6px 16px; cursor:pointer; font-size:0.85rem;">
      <%= t('sca.guided.dismiss') %>
    </button>
  </div>
  ```
- Add localStorage JS at the bottom (in the script block):
  ```javascript
  if (localStorage.getItem('sca-intro-dismissed')) {
    var b = document.getElementById('sca-intro-banner');
    if (b) b.style.display = 'none';
  }
  function dismissBanner() {
    localStorage.setItem('sca-intro-dismissed', '1');
    document.getElementById('sca-intro-banner').style.display = 'none';
  }
  ```

**C. Add difficulty badges on finding cards (SCAC-03):**
Each finding now has a `f.difficulty` field ('easy', 'medium', 'advanced') from the route handler.
- Add a difficulty badge inline next to the existing severity and CWE badges
- Use traffic-light colors per CONTEXT.md:
  - easy: bg #e8f8e8, color #1e8449
  - medium: bg #fff0e0, color #c0732a
  - advanced: bg #ffe0e0, color #c0392b
- Use translated label via a lookup:
  ```ejs
  <%
    const diffColors = {
      easy: { bg: '#e8f8e8', color: '#1e8449' },
      medium: { bg: '#fff0e0', color: '#c0732a' },
      advanced: { bg: '#ffe0e0', color: '#c0392b' }
    };
    const diffLabel = {
      easy: t('sca.difficulty.easy'),
      medium: t('sca.difficulty.medium'),
      advanced: t('sca.difficulty.advanced')
    };
  %>
  ```
- Place the badge after the CWE badge in each finding card's badge row:
  ```ejs
  <span class="badge-sm" style="background:<%= diffColors[f.difficulty].bg %>; color:<%= diffColors[f.difficulty].color %>;"><%= diffLabel[f.difficulty] %></span>
  ```
- Define diffColors/diffLabel ONCE at the top of the forEach loop or outside it

**D. Translate AJAX feedback messages (TRAN-10):**
In the `<script>` block, embed translated strings as JS variables via EJS:
```ejs
const MSG_SAVING = '<%= t("sca.common.saving") %>';
const MSG_SAVED = '<%= t("sca.common.savedDraft") %>';
const MSG_SUBMITTED = '<%= t("sca.common.submittedSuccess") %>';
const MSG_ERROR = '<%= t("sca.common.errorSaving") %>';
const MSG_NETWORK = '<%= t("sca.common.networkError") %>';
```
Then update saveReview() to use these constants instead of hardcoded English:
- `msg.textContent = 'Saving...'` -> `msg.textContent = MSG_SAVING;`
- `'Submitted!'` -> `MSG_SUBMITTED`
- `'Draft saved.'` -> `MSG_SAVED`
- `'Error saving review.'` -> `MSG_ERROR`
- `'Network error...'` -> `MSG_NETWORK`

Preserve all existing functionality: toggleForm, saveReview AJAX logic, FormData handling, reload behavior.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('./views/sca/student-lab.ejs','utf8'); const checks=['sca.studentLab.title','sca.guided.introBannerTitle','sca.guided.dismiss','localStorage','sca-intro-dismissed','diffColors','difficulty','MSG_SAVING','MSG_SUBMITTED','sca.common.truePositiveDesc','sca.common.falsePositive','sca.common.needsInvestigation']; let ok=true; for(const c of checks){if(!src.includes(c)){console.log('FAIL: missing '+c);ok=false;}} if(!src.includes('Static Code Analysis')&&!src.includes('Start Review')&&!src.includes('Draft saved.'))console.log('PASS: no hardcoded English found'); if(ok)console.log('PASS: all required elements present');"</automated>
  </verify>
  <done>student-lab.ejs has zero hardcoded English strings (all use t() calls), displays a dismissible blue intro banner with localStorage persistence, shows color-coded difficulty badges on every finding card, and uses translated AJAX feedback messages for save/submit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire finding-detail.ejs with t() calls, difficulty badge, and collapsible hints</name>
  <files>views/sca/finding-detail.ejs</files>
  <action>
Rewrite finding-detail.ejs to replace ALL hardcoded English strings with t() calls and add two new features. Structure and styling remain the same.

**A. Replace all hardcoded English with t() calls (TRAN-03, TRAN-09):**

| Current English | Replace with |
|----------------|--------------|
| "SCA Lab" (back link) | `<%= t('sca.findingDetail.backToLab') %>` |
| Page title h1 `finding.title` | Already localized by route handler -- no change needed |
| "Location" h3 | `<%= t('sca.findingDetail.location') %>` |
| "Code Snippet" h3 | `<%= t('sca.findingDetail.codeSnippet') %>` |
| "Description" h3 | `<%= t('sca.findingDetail.description') %>` |
| "Remediation Guidance" h3 | `<%= t('sca.findingDetail.remediationGuidance') %>` |
| "Student Reviews" h3 | `<%= t('sca.findingDetail.studentReviews') %>` |
| "Student #" prefix | `<%= t('sca.instructor.student') %> #` |
| "Notes:" label | `<%= t('sca.studentDetail.notes') %>:` |
| "Remediation:" label | `<%= t('sca.findingDetail.proposedRemediation') %>:` |
| "Status:" label | `<%= t('sca.instructor.status') %>:` |
| "Submitted" (in status line) | `<%= t('sca.studentLab.submitted') %>` |
| "Your Review" h3 | `<%= t('sca.findingDetail.yourReview') %>` |
| "Submitted on" | `<%= t('sca.findingDetail.submittedOn') %>` |
| "Classification" label | `<%= t('sca.findingDetail.classification') %>` |
| "-- select --" | `<%= t('sca.findingDetail.selectOption') %>` |
| "True Positive" | `<%= t('sca.common.truePositive') %>` |
| "False Positive" | `<%= t('sca.common.falsePositive') %>` |
| "Needs Investigation" | `<%= t('sca.common.needsInvestigation') %>` |
| "Your Notes" label | `<%= t('sca.findingDetail.yourNotes') %>` |
| "Proposed Remediation" label | `<%= t('sca.findingDetail.proposedRemediation') %>` |
| "Save Draft" button | `<%= t('sca.findingDetail.saveDraft') %>` |
| "Submit" button | `<%= t('sca.findingDetail.submit') %>` |
| "Vulnerability Manager" h3 | `<%= t('sca.findingDetail.vulnerabilityManager') %>` |
| "Imported to VM as" | `<%= t('sca.findingDetail.importedToVM') %>` |
| "This finding has not yet been imported..." | `<%= t('sca.findingDetail.notImported') %>` |
| "Push to VM" button | `<%= t('sca.findingDetail.pushToVM') %>` |
| "References" h3 | `<%= t('sca.findingDetail.references') %>` |
| "Tool:" badge prefix | `<%= t('sca.findingDetail.tool') %>:` |

Also translate the `confirm()` dialog in the importFinding script:
```javascript
if (!confirm('<%= t("sca.findingDetail.pushToVM") %>: "<%= finding.title %>"?')) return;
```

**B. Add difficulty badge (SCAC-03):**
Add a difficulty badge in the badge row (alongside severity, CWE, category, tool badges):
```ejs
<%
  const diffColors = {
    easy: { bg: '#e8f8e8', color: '#1e8449' },
    medium: { bg: '#fff0e0', color: '#c0732a' },
    advanced: { bg: '#ffe0e0', color: '#c0392b' }
  };
  const diffLabel = {
    easy: t('sca.difficulty.easy'),
    medium: t('sca.difficulty.medium'),
    advanced: t('sca.difficulty.advanced')
  };
%>
<span class="badge-sm" style="background:<%= diffColors[finding.difficulty].bg %>; color:<%= diffColors[finding.difficulty].color %>;"><%= diffLabel[finding.difficulty] %></span>
```
Place after the Tool badge in the badge row.

**C. Add collapsible hints section (SCAC-04):**
Per CONTEXT.md locked decision:
- Collapsible "Besoin d'aide ?" section on finding-detail page ONLY
- Placed BELOW the description section, BEFORE remediation guidance
- 2-3 guiding analysis questions per finding using hint keys from fr.json

Implementation:
```ejs
<div style="margin-bottom:1rem;">
  <button onclick="toggleHints()" type="button" style="background:none; border:1px solid #dee2e6; border-radius:4px; padding:6px 14px; cursor:pointer; color:#666; font-size:0.9rem;">
    Besoin d'aide ?
  </button>
  <div id="hints-section" style="display:none; margin-top:0.75rem; padding:0.75rem; background:#f8f9fa; border-radius:6px; border-left:3px solid #002855;">
    <strong style="color:#002855; font-size:0.9rem;">Pistes d'analyse</strong>
    <ul style="margin:0.5rem 0 0; padding-left:1.25rem; font-size:0.9rem; line-height:1.8; color:#333;">
      <li><%= t('sca.findings.' + finding.id + '.hint1') %></li>
      <li><%= t('sca.findings.' + finding.id + '.hint2') %></li>
      <% if (t('sca.findings.' + finding.id + '.hint3') !== 'sca.findings.' + finding.id + '.hint3') { %>
        <li><%= t('sca.findings.' + finding.id + '.hint3') %></li>
      <% } %>
    </ul>
  </div>
</div>
```

The hint3 check uses the fact that t() returns the key itself when not found -- so we compare the return value to the key string to conditionally render it.

Add the toggleHints function in the script block:
```javascript
function toggleHints() {
  var el = document.getElementById('hints-section');
  el.style.display = el.style.display === 'none' ? 'block' : 'none';
}
```

Place this section between the Description `</p>` and the Remediation `<h3>`. Only show for student role:
```ejs
<% if (user.role === 'student') { %>
  <!-- hints section here -->
<% } %>
```

Preserve all existing functionality: form submission, importFinding, instructor review display, VM status display.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('./views/sca/finding-detail.ejs','utf8'); const checks=['sca.findingDetail.backToLab','sca.findingDetail.location','sca.findingDetail.codeSnippet','sca.findingDetail.description','sca.findingDetail.remediationGuidance','sca.findingDetail.yourReview','sca.findingDetail.classification','sca.common.truePositive','sca.common.falsePositive','sca.common.needsInvestigation','diffColors','difficulty','toggleHints','hints-section','hint1','hint2','Besoin']; let ok=true; for(const c of checks){if(!src.includes(c)){console.log('FAIL: missing '+c);ok=false;}} if(ok)console.log('PASS: all required elements present'); const en=['Location','Code Snippet','Description','Remediation Guidance','Your Review','Classification','True Positive','False Positive','Needs Investigation','Your Notes','Proposed Remediation','Save Draft','Push to VM','Vulnerability Manager','References']; for(const e of en){if(src.includes('>'+e+'<')){console.log('WARN: possible hardcoded English: '+e);}}"</automated>
  </verify>
  <done>finding-detail.ejs has zero hardcoded English strings (all use t() calls), displays a difficulty badge in the badge row, shows classification dropdown with French labels (Vrai positif, Faux positif, Necessite une investigation), and has a collapsible "Besoin d'aide ?" section with per-finding hint questions visible only to students.</done>
</task>

</tasks>

<verification>
1. Start the server: `npm start` (or `node server.js`)
2. Login as student (alice_student / student123)
3. Navigate to /sca -- verify:
   - Page title, subtitle, progress text all in French
   - Blue intro banner visible with "Comment aborder cet exercice" heading
   - Dismiss banner -- reload page -- banner stays hidden
   - Each finding card has a difficulty badge (green Facile, orange Moyen, red Avance)
   - Findings sorted: Facile group first, then Moyen, then Avance
   - Click "Commencer l'analyse" on a finding -- inline form shows French labels
   - Classification dropdown has French options
   - Save draft -- feedback message in French
4. Navigate to /sca/findings/1 -- verify:
   - All labels in French
   - Difficulty badge visible
   - "Besoin d'aide ?" button visible -- click to expand hints
   - 2-3 guiding questions appear
   - Classification dropdown in French
   - Back link says "Laboratoire ACS"
</verification>

<success_criteria>
- student-lab.ejs: zero hardcoded English strings, intro banner with localStorage dismiss, difficulty badges, French AJAX feedback
- finding-detail.ejs: zero hardcoded English strings, difficulty badge, collapsible hints section, French classification dropdown
- All 8 phase requirements addressed across Plans 01 + 02
</success_criteria>

<output>
After completion, create `.planning/phases/03-sca-student-experience/03-02-SUMMARY.md`
</output>
