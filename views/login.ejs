<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - HEC Montréal Application Security</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
      background: #002855;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
    }

    .login-container {
      background: white;
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
      padding: 3rem;
      width: 100%;
      max-width: 450px;
    }

    h1 {
      text-align: center;
      color: #002855;
      margin-bottom: 0.5rem;
      font-size: 1.8rem;
    }

    .subtitle {
      text-align: center;
      color: #7f8c8d;
      margin-bottom: 2rem;
      font-size: 0.95rem;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    label {
      display: block;
      margin-bottom: 0.5rem;
      color: #002855;
      font-weight: 500;
    }

    input {
      width: 100%;
      padding: 0.75rem;
      border: 2px solid #e0e0e0;
      border-radius: 6px;
      font-size: 1rem;
      transition: border-color 0.2s;
    }

    input:focus {
      outline: none;
      border-color: #002855;
    }

    .btn {
      width: 100%;
      padding: 0.875rem;
      background: #002855;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s;
    }

    .btn:hover {
      background: #001a3d;
    }

    .alert-danger {
      background: #fef2f2;
      color: #991b1b;
      border: 1px solid #fecaca;
      padding: 1rem;
      border-radius: 8px;
      margin-bottom: 1.5rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .alert-danger::before {
      content: "⚠️";
      font-size: 1.25rem;
    }

    .alert-danger-text {
      flex: 1;
    }

    .alert-danger-title {
      font-weight: 600;
      margin-bottom: 0.25rem;
    }

    .alert-danger-message {
      font-size: 0.9rem;
      opacity: 0.9;
    }

    .demo-accounts {
      margin-top: 2rem;
      padding-top: 2rem;
      border-top: 1px solid #e0e0e0;
    }

    .demo-accounts h3 {
      color: #002855;
      font-size: 1rem;
      margin-bottom: 0.75rem;
    }

    .demo-accounts p {
      color: #555;
      font-size: 0.9rem;
      margin-bottom: 0.5rem;
      padding: 0.5rem;
      background: #f8f9fa;
      border-radius: 4px;
      font-family: monospace;
    }

    .info-text {
      text-align: center;
      color: #7f8c8d;
      font-size: 0.85rem;
      margin-top: 1.5rem;
    }

  </style>
</head>
<body>
  <div class="login-container">
    <div style="text-align: center; margin-bottom: 1.5rem;">
      <img src="/images/hec-logo.svg" alt="HEC Montréal" style="height: 80px; width: 80px; border-radius: 8px;">
    </div>
    <h1>HEC Montréal</h1>
    <p class="subtitle">Application Security Learning Platform</p>

    <% if (error) { %>
      <div class="alert-danger">
        <div class="alert-danger-text">
          <div class="alert-danger-title">Login Failed</div>
          <div class="alert-danger-message"><%= error %></div>
        </div>
      </div>
    <% } %>

    <form action="/auth/login" method="POST">
      <div class="form-group">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" required autofocus>
      </div>

      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" required>
      </div>

      <button type="submit" class="btn">Login</button>
    </form>

    <div class="demo-accounts">
      <h3>Default Accounts:</h3>
      <p><strong>Admin:</strong> admin / admin123</p>
      <p><strong>Professor:</strong> prof_jones / prof123</p>
      <p><strong>Student:</strong> alice_student / student123</p>
    </div>

    <p class="info-text">
      Educational application security demonstration platform
    </p>
  </div>
</body>
</html>
