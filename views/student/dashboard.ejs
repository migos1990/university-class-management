<%- include('../partials/header') %>

<div class="page-header">
  <h1 class="page-title">My Classes</h1>
  <p class="page-subtitle">View your enrolled classes and grades</p>
</div>

<div class="card">
  <h2>Enrolled Classes</h2>

  <% if (enrollments.length === 0) { %>
    <div style="text-align: center; padding: 3rem; color: #666;">
      <div style="font-size: 3rem; margin-bottom: 1rem;">📚</div>
      <p>You are not enrolled in any classes yet.</p>
    </div>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Code</th>
          <th>Class Name</th>
          <th>Semester</th>
          <th>Grade</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% enrollments.forEach(enrollment => { %>
          <tr>
            <td><strong><%= enrollment.code %></strong></td>
            <td><%= enrollment.name %></td>
            <td><%= enrollment.semester %></td>
            <td>
              <% if (enrollment.grade) { %>
                <span style="background: #d4edda; padding: 4px 12px; border-radius: 4px; color: #155724; font-weight: 600;">
                  <%= enrollment.grade %>
                </span>
              <% } else { %>
                <span style="background: #fff3cd; padding: 4px 12px; border-radius: 4px; color: #856404;">
                  In Progress
                </span>
              <% } %>
            </td>
            <td>
              <a href="/classes/<%= enrollment.class_id %>" class="btn btn-primary" style="font-size: 0.85rem; padding: 0.4rem 0.8rem;">
                View Details
              </a>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } %>
</div>

<div class="card">
  <h2>About This Platform</h2>
  <p style="color: #666; line-height: 1.6;">
    This platform demonstrates various application security concepts. Notice the security status bar at the top
    showing which security features are currently enabled. Ask your instructor to toggle features to see how
    they affect the application's behavior and data storage.
  </p>
</div>

<%- include('../partials/footer') %>
